# Ask for a name, then read one line from standard input.
# `inveure` returns the line without its newline.

aafdrökke("What is your name?")
loat naam = inveure()

es naam:
    aafdrökke("Hello, " + naam)
angesj:
    aafdrokke("Hello, stranger")
enj
